# Projet 1 : Mini-Airbnb - Plateforme d'Annonces de Logements

# 🏡 AirAndBitume - "Entre ciel et route, votre chez-vous est partout."

## 🚀 Pour lancer le projet

### ✅ Prérequis

- Avoir **Node.js** et **npm** installés.
- Se rendre dans le **dossier du projet** via un terminal.


### 📦 Installation des dépendances

> #### Les dépendances utilisées sont :  
> #### `dotenv`, `express`, `morgan`, `cookie-parser` et `ejs`.

### Dans le terminal, exécutez l’une des commandes suivantes :

```powershell
npm install
```
Ou 

```powershell
npm run project
```

## ▶️ Lancer le serveur

### Après avoir installé les dépendances et être dans le dossier du projet, lancez le serveur avec :

```
npm start
```
 ### Le serveur sera généralement accessible à l’adresse suivante :
 ### 👉 http://localhost:3000

## Mais si vous avez un `.env` le projet permet de lancer le serveur sur la variable `PORT` du `.env`, le `.env` doit être placé à la racine du projet